using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class snapCamera : MonoBehaviour
{
    /*public GameObject sceneSize;
    public Camera mainCamera;
    public float sizeCamera;
    private float getSceneSize;*/
    // Start is called before the first frame update
    void Start()
    {
        /*getSceneSize = sceneSize.GetComponent<Transform>().localScale.x;
        mainCamera.orthographic = true;
        mainCamera.orthographicSize = sizeCamera;
        Debug.Log(getSceneSize); */
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
